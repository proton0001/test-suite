set(POLYBENCH_UTILS SingleSource/Benchmarks/Polybench/utilities )
set(PROG blur)
list(APPEND CFLAGS -I ${CMAKE_SOURCE_DIR}/${POLYBENCH_UTILS} -DPOLYBENCH_DUMP_ARRAYS -fpermissive --std=c++11)
list(APPEND LDFLAGS -lm -ljpeg -lstdc++)
file(COPY Sample.jpeg DESTINATION "${CMAKE_CURRENT_BINARY_DIR}")
# if(SMALL_PROBLEM_SIZE)
#   list(APPEND CFLAGS -DSMALL_DATASET)
# endif()
set(HASH_PROGRAM_OUTPUT 1)
add_definitions(-DFP_ABSTOLERANCE=1e-5)
llvm_singlesource()
